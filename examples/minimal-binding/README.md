# Minimal binding, filled in

One fictional operator, every file filled, nothing padded. Copy the shape, not the words. This is what `/onboard` produces for a solo founder in about ten minutes.

The operator: **Ledgerline**, a bookkeeping app for freelancers, run by one person.
